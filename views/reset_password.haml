!!!
%html
  %head
    %meta(charset="utf-8")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title Academian | Ändra lösenordet
    %link{href: "/css/bootstrap.css", rel: :stylesheet}
  %body
    .container
      .row
        .col-md-8.col-md-offset-2
          -flash.keys.each do |key|
            %div{class: "alert alert-#{key}", role: :alert}
              = flash[key]
      .row
        .col-md-8.col-md-offset-2
          %form.form-horizontal{method: :post}
            != csrf_token_tag
            .control-group
              %label.control-label E-mail:
              %input.form-control{name: 'email', type: 'email', value: (params[:email] || '').strip.gsub(/ /, '+'), readonly: params[:email]}
            .control-group
              %label.control-label Password:
              %input.form-control{name: 'password', type: 'password', required: true}
            .control-group
              %label.control-label Confirm password:
              %input.form-control{name: 'password2', type: 'password', required: true}
            .form-actions
              %input.btn.btn-primary{type: 'submit', value: 'Submit'}


