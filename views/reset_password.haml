!!!
%html
  %head
    %meta(charset="utf-8")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title Academian | Ändra lösenordet
    %link{href: asset_path("/css/site.css"), rel: :stylesheet}
  %body
    .container-narrow
      - flash.keys.each do |key|
        %div{class: "alert alert-#{key}", role: :alert}
          = flash[key]
      %form{method: :post}
        .form-group
          %label E-mail:
          %input{name: 'email', type: 'email', value: (params[:email] || '').strip.gsub(/ /, '+'), readonly: params[:email]}
        .form-group
          %label Password:
          %input{name: 'password', type: 'password', required: true}
        .form-group
          %label Confirm password:
          %input{name: 'password2', type: 'password', required: true}
        %input.btn.btn-primary{type: 'submit', value: 'Submit'}
