.text-center
  .row
    %h1
      Välkommen till
      %br
      %small  Academia Vestigia Nuda Sinistri Pedis
    %p
      Här kan vi skriva en hälsning från CdC till alla medlemmar
  .row
    .col-sm-12
      %h3 Kommande event
  .row
    .col-sm-offset-3.col-sm-6
      .list-group
      -@parties.each do |p|
        .list-group-item
          %h4.list-group-item-heading
            %a{href: "/party/#{p.id}"}= p.description
          .list-group-item-text
            -if p.is_attending?(@user.id)
              Du är anmäld!
            -else
              !=haml(:attend_form, layout: false, locals: { party_id: p.id, a: @user.attendance(p.id) })
