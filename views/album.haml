.container-narrow.text-center
  %h1= @album.title
#grid{'data-columns': ''}
  - @photos.each do |p|
    %div
      %a{href: url("/#{p.album_id}/#{p.id}")}
        %img{src: p.original_temp, width: '300'}
!= #haml(:photoswipe, layout: false)
%script{src: "/js/salvattore.min.js"}
